# The correct way 
user_promt = "enter the task: "

todos [] #empty listoutside the loop 

while True:
  todo = input(user_promt)  
  todos.append(todo) #add all todo to a list with todos
  
# if place user promt inputinside loop 


todos [] #empty listoutside the loop 

while True:
  user_promt = "enter the task: " # gives the same outputbut the code is heavier
  todo = input(user_promt)  
  todos.append(todo) #add all todo to a list with todos
